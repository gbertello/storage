version: '3'
services:
  node_test:
    container_name: storage_node_test
    build: node/
    ports:
      - 81:80
    volumes:
      - "./storage:/app/storage"
    networks:
      - test
networks:
  test:
    driver: bridge